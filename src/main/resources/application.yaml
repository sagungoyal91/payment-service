server:
  port: 8083
spring:
  application:
    name: payment-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/payment_db
management:
  endpoints:
    web:
      exposure:
        include: "*"

